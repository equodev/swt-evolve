swtVersionFull=3.134.0.v20260515-1429

# The oldest JDK the SWT release above supports; swt_native compiles at this level (see its
# build.gradle.kts). It has to live here rather than only in gradle/versions/{ver}.properties,
# because that file belongs to the parent build and swt-evolve is an included build -- the parent's
# allprojects never reaches it. syncSwtEvolveProperties rewrites the keys this file already has, so
# the key must exist for a per-version build to receive it.
minJavaVersion=21
eclipseUrl=https://download.eclipse.org/releases/2026-06/202606101000

# Eclipse platform dependency versions (Eclipse 4.39)
jfaceVersion=3.39.100
jfaceTextVersion=3.31.0
draw2dVersion=3.23.0.202605171958
gefVersion=3.26.0.202605171956
coreCommandsVersion=3.12.500
equinoxCommonVersion=3.20.400
eclipseTextVersion=3.14.700

# NatTable
nattableVersion=2.5.0
